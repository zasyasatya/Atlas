# Deployed corrosion segmentation app.
# CPU wheels only: the app runs inference on one image at a time, and a GPU
# base image is ~6x larger for no useful gain here.
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0
numpy>=1.24
pillow>=10.0
streamlit>=1.36
pandas>=2.0
