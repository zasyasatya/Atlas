# Training + inference
torch>=2.0
numpy>=1.24
pillow>=10.0

# Deployment app
streamlit>=1.30
pandas>=2.0

# Install torch for your platform first if you need CUDA:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# CPU-only:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
